import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.baby',
  appName: 'Baby Stimulation: High Contrast Images',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
